cmake_minimum_required(VERSION 3.4.1)

project(json)   

add_definitions(-DOS_ANDROID)   

set(SOURCE_LIB
<%= @srcList %>
)

include_directories(<%= @rhoRoot %>/platform/shared)
include_directories(<%= @rhoRoot %>/platform/shared/json.new)

add_library(json STATIC ${SOURCE_LIB})

