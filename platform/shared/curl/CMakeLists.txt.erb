cmake_minimum_required(VERSION 3.4.1)

project(curl)      

add_definitions(-DHAVE_CONFIG_H -DOS_ANDROID)

set(SOURCE_LIB
<%= @srcList %>
)

include_directories(<%= @rhoRoot %>/platform/shared/curl/include)
include_directories(<%= @rhoRoot %>/platform/shared/curl/lib)
include_directories(<%= @rhoRoot %>/platform/shared)

add_library(curl STATIC ${SOURCE_LIB})

