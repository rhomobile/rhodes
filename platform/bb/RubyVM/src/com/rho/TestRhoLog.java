package com.rho;

public class TestRhoLog {
/*
	private static final RhoLogger LOG = RhoLogger.RHO_STRIP_LOG ? new RhoEmptyLogger() : 
			new RhoLogger("TestRhoLog");
	
	void testClassLog(){
		LOG.INFO( "testClassLog:" + 125 );
	}

	void testTraceLog(){
		if ( RhoLogger.RHO_DEBUG )
			LOG.TRACE( "Trace shows only in Debug mode" );
	}

	void testAssert(){
		LOG.ASSERT(false,"Should be non false");
	}
	
	public void runAllTests(){
		testClassLog();
		testTraceLog();
		//testAssert();
	}*/
}
